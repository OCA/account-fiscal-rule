Adds a product domain on fiscal positions so tax mapping lines can be applied only
for products matching that domain, only for products not matching that domain, or
always when the line leaves the domain filter empty.
