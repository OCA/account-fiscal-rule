This module extends the OCA 'Product Fiscal Classification' module to make
HR Expense product form view working, if 'HR Expense' is installed.

By default, HR Expense define a new product form view. With that
glue module, the form view brings fiscal classification field,
and supplier taxes are readonly.
